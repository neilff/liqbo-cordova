/**
 *  Stores: Request Service
 */
angular.module('lcboApp.services')
    .factory('DrinksService', function() {

        var getStores = function(config) {
            return 'hello world';
        }

        return {
            getStores: getStores
        }
    });
